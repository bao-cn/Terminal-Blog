# Optional. When omitted, the first database initializes root with password "root".
# Set a high-entropy value before the first production startup.
TERMINAL_ROOT_PASSWORD=replace-with-a-random-secret-at-least-16-characters
